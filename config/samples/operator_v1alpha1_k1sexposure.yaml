apiVersion: operator.k1s.io/v1alpha1
kind: K1sExposure
metadata:
  name: echo
  namespace: k1s-operator-system
spec:
  clusterRef:
    name: edge-a
  appRef:
    namespace: default
    name: echo
  host: echo.apps.edge-a.core.home.arpa
  path: /
  service:
    name: echo-k1s
    port: 80
  ingress:
    enabled: true
    className: nginx

apiVersion: apps/v1
kind: Deployment
metadata:
  name: k1s-operator-controller-manager
  labels:
    app.kubernetes.io/name: k1s-operator
    app.kubernetes.io/component: controller-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: k1s-operator
      app.kubernetes.io/component: controller-manager
  template:
    metadata:
      labels:
        app.kubernetes.io/name: k1s-operator
        app.kubernetes.io/component: controller-manager
    spec:
      serviceAccountName: k1s-operator-controller-manager
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: manager
        image: controller:v0.1.0-dev
        imagePullPolicy: IfNotPresent
        args:
        - --leader-elect
        - --metrics-bind-address=127.0.0.1:8080
        - --resourceset-allowed-kinds=$(RESOURCESET_ALLOWED_KINDS)
        env:
        - name: WATCH_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: RESOURCESET_ALLOWED_KINDS
          value: Deployment,InferenceCell,InferenceCellSet
        ports:
        - name: metrics
          containerPort: 8080
          protocol: TCP
        - name: health
          containerPort: 8081
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
        livenessProbe:
          httpGet:
            path: /healthz
            port: health
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsGroup: 65532
          runAsNonRoot: true
          runAsUser: 65532
          capabilities:
            drop:
            - ALL
